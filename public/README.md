# Public assets (copied to dist on export)

- **manifest.json** – PWA manifest; used by the build.
- **icon-192.png**, **icon-512.png** – Add these for “Add to Home Screen” icons (192×192 and 512×512 PNG). If missing, the app still runs; install may use a generic icon.
