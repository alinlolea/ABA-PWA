/**
 * Tablet-first touch targets (60–80px) for ABA therapy sessions.
 * Use for buttons, nav items, and trial response controls.
 */
export const TouchTarget = {
  /** Minimum size for interactive elements (px). */
  minSize: 72,
} as const;
